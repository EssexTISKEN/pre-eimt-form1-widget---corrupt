document.querySelectorAll('.accordion .item').forEach(item=>{const h=item.querySelector('.head');const c=item.querySelector('.content');h.onclick=()=>{const o=item.classList.contains('open');document.querySelectorAll('.accordion .item').forEach(i=>{i.classList.remove('open');i.querySelector('.content').style.maxHeight='0';});if(!o){item.classList.add('open');c.style.maxHeight=c.scrollHeight+'px';}}});